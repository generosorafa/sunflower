name: Validate static site

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Check JavaScript syntax
        run: |
          node --check data.js
          node --check app.js
          node --check app-base.js
          node --check flowers-v3.js
      - name: Check required files
        run: |
          test -f index.html
          test -f styles.css
          test -f styles-base.css
          test -f data.js
          test -f app.js
          test -f app-base.js
          test -f flowers-v3.js
